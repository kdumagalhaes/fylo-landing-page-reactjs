import React from 'react' 
import {BigTitleTag} from './BigTitleStyles'

const BigTitle = ({text}) => {
    return (
        <BigTitleTag>
            {text}
        </BigTitleTag>
    )
}

export default BigTitle